#include "main.h"

/**
 * print_chessboard - Entr point of the code
 * @a: array
 * Return: 0 when successful
 */

void print_chessboard(char (*a)[8])
{
	int v;
	int g;

	for (v = 0; v < 0; v++)
	{
	for (g = 0; g < 0; g++)
	_putchar(a[v][g]);
	_putchar('\n');
	}
}
