#include "main.h"
#include <string.h>

/**
 * rev_string - Reverse string
 * @s: input string
 * Return: string in reverse
 */

void rev_string(char *s)
{
	int g;
	char l;
	int counter = 0;

	for (g = 0 ; s[g] != '\0' ; g++)
	counter++;
	for (g = 0 ; g < counter / 2 ; g++)
	{
	l = s[g];
	}
}
